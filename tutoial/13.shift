$0至$9表示参数位置, 其中$0表示当前执行进程的文件名字.
例如:
./args.sh arg1 arg2 arg3

其中$1指向arg1...以此类推.

可以通过shift命令, 移动参数的位置,从而改变$[1-9]的指向.

除了$0外, 把所有的参数都向左移动一个位置, 
原来$1对应第一个参数,那么参数往左移动之后, $1可以对应第二个参数.

多移动几次,$9对应的参数可以超过10(不移动的时候,$9对应第9个参数)
(当然移动之后,最左端的参数就被$num丢失标记了)


左移参数,就好像参数出队了, $1永远指向队首.