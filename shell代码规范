1. 第二行,写注释,表明当前的脚本名字.

shell脚本的一般流程:
1. 编辑shell脚本
2. 将该文件命名为.sh结尾
3. 增加脚本文件的可执行权限
4. 使用相对路径执行脚本文件(例如./test.sh)


shell脚本中变量名大小写敏感

三种引用及其区别:
1. "$sum"
2. ${sum}
3. $sum


表达式求值,数学运算:

1. expr 
   例如: expr 3%2
   (但是注意, *要转义成\*)